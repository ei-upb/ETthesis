% Custom packages and configuration for emtthesis.
% Put your configuration here instead of the main thesis.tex.

%% Page format
% \KOMAoptions{BCOR=12mm} % Optionally adjust binding correction.

%% Additional math packages
\usepackage{amsmath} % e.g. align environment
\usepackage{amsopn} % support custom math operators
\usepackage{amssymb} % additional math fonts
\usepackage{trfsigns} % symbols for transformation pairs etc.

%% Properly formatted physical units
\usepackage[
    list-final-separator={ und },
    list-pair-separator={ und },
    range-phrase={ bis },
    output-decimal-marker={,}
]{siunitx}

%% Bibliography
\addbibresource{thesis.bib}

% Auto-detection for own publications
% Matches all publications with the given author, adding a keyword "own". For
% doctoral theses in submission mode, own publications are set in a separate
% bibliography.
% Note that the entire name given below is matched, so if you specify something
% like "Max Mustermann", it will not match "Mustermann, Max".
\autodetectOwnPubs{Mustermann}

%% Figures & plotting
% TikZ
\usepackage{tikz}

% PGFplots
\usepackage{pgfplots}
\pgfplotsset{compat=newest}

% TikZ externalize
% Enable when desired. Note that the "tikz" subdirectory is not automatically
% created! If you get an error like "can't write to file ...", the directory
% is propably missing.
\usetikzlibrary{external}
% \tikzexternalize[prefix=tikz/]

% Optional helper command for inputting tikz files.
% Use as \tikzinput{foobar} for a file "figures/foobar.tikz", i.e. omit the
% path prefix and file extension.
% This results in externalized PDF documents being named according to the
% original file instead of numbered like "thesis-figure0.pdf".
% Note: Requires the "external" tikz library!
\newcommand*{\tikzinput}[1]{%
    \tikzsetnextfilename{#1}
    \input{figures/#1.tikz}
}
